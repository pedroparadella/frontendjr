import styled from "styled-components"

export const Container = styled.header`
    background: transparent linear-gradient(272deg, var(--secondary-color) 0%, var(--primary-color) 100%) 0% 0% no-repeat padding-box;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1rem 2rem;
`
export const Content = styled.div`
    width: 100%;
    margin: 0 auto;

    display: flex;
    align-items: center;
`